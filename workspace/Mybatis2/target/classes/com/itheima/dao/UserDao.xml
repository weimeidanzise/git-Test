<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.dao.UserDao">
    <select id="findTotal" resultType="int">
        SELECT count(*) from user
    </select>
    <insert id="addUser">
    <selectKey keyProperty="id" keyColumn="id" resultType="int" order="AFTER">SELECT LAST_insert_id();</selectKey>
        INSERT  into user (id,name,age,gender) values (null,#{name},#{age},#{gender})
    </insert>
    <select id="findByVo" parameterType="QueryVo" resultType="User">
        SELECT * from user where name like #{user.name}
    </select>

    <select id="findAll" resultMap="userMap">
        SELECT * from user
    </select>

    <select id="findByName" resultType="user" parameterType="STRING">
        SELECT * from user where name like #{Name}
    </select>
    <resultMap id="userMap" type="User">
        <id property="id" column="id"></id>
        <result property="name" column="name"></result>
        <result property="age" column="age"></result>
        <result property="gender" column="gender"></result>
    </resultMap>

</mapper>